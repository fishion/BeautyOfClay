Set to send contact form requests to monir

Add 'Events' page, like a contact page, to collect order details - driven by JSON list of events & prices?

convert to REM and use CSS variables for better responsive scaling 
